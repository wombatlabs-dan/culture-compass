## Packages
framer-motion | Smooth spring animations for notification bubbles and transitions
date-fns | Formatting dates for the lock screen clock widget
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
The backend provides /api/vibe/generate for the AI rules.
The UI mimics a mobile Lock Screen.
